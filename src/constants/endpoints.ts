enum Endpoints {
  SIGN_IN = '/auth/signin',
  SIGN_UP = '/auth/signup',
  USERS = '/users',
  BOARDS = '/boards',
  COLUMNS = '/columns',
  COLUMNS_SET = '/columnsset',
  TASKS = '/tasks',
  TASKS_SET = '/tasksset',
}

export default Endpoints;

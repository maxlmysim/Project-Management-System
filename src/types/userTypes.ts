export interface IUserData {
  name: string;
  login: string;
}

export interface IUser {
  password: string;
  login: string;
}

export interface INewUser {
  name: string;
  password: string;
  login: string;
}

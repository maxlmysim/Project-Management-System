export interface UserData {
  name: string;
  login: string;
}

export interface User {
  password: string;
  login: string;
}

export interface NewUser {
  name: string;
  password: string;
  login: string;
}
